My own version of the simon example project.

Deploy file: takes the key, hostname, and service from the command line. Checks that all parameters were passed in. Clears all files previously in the server. Copies new files onto server.

./deployFiles.sh -k keypath -h domain -s simon

